# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Ignore plan output files
*.tfplan
*.out
plan.out

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore TFLint cache
.tflint.d/

# Ignore terraform lock files (users will generate their own when using modules)
.terraform.lock.hcl

# Ignore lock files in modules (users will generate their own)
terraform/*/.terraform.lock.hcl
terraform/*/tests/*/.terraform.lock.hcl

# Ignore any local test files or scratch directories
scratch/
.scratch/

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
